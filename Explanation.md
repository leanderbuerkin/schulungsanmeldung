
Why immutable data-structures?

    """
    If the JuLeis get more levels to separate their wishes into,
    they can communicate their choices better.

    But more levels make it harder for the program to give all JuLeis a slot:
    The program can not give another JuLei another slot from a lower level,
    because this would penalize JuLeis that provide many wishes.
    """


    """
To make the process more reliable and easier to understand
each data-type is read-only/immutable/frozen:

- MappingProxyType is a read-only dict
"""
        """The more wishes, the more complicated the problem is."""